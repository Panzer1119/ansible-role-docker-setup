---
# Default variables for Docker setup
use_gelf_logging: false
gelf_address: "tcp://graylog:12201"

zfs_pool_name: "docker"

use_zfs_storage_driver: false
zfs_docker_storage_driver_dataset: "docker/storage"

use_zfs_volume_plugin: false
docker_zfs_plugin_version: "v1.0.5"
zfs_docker_volume_plugin_dataset: "docker/volumes"
